/* 
돋도 못한 사람의 수 N (1 <= N <= 500,000)
보도 못한 사람의 수 M (1 <= M <= 500,000)
*/

#include <iostream>
#include <string>

using namespace std;

class Set {
  enum DataEnv {
    kDefaultSize = 10000
  };
  struct Data {
    int key;
    string val;
  }
  
}


int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  

  return 0;  
}